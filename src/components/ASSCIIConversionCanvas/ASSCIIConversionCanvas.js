import {Paper} from "@mui/material";

const ASSCIIConversionCanvas = ({ image }) => {
  return (
    //TODO: Replace this with canvas for image manipulation

      <img src={image} alt="Waiting for image..." />


);
};

export default ASSCIIConversionCanvas;
